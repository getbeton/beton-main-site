---
import Card from '../ui/Card.astro';

interface Props {
  industry: {
    id: string;
    data: {
      title: string;
      description: string;
    };
  };
}

const { industry } = Astro.props;
---

<Card href={`/industries/${industry.id}`}>
  <h3 class="text-lg font-semibold mb-2">{industry.data.title}</h3>
  <p class="text-sm text-[var(--color-text-secondary)] leading-relaxed mb-4">
    {industry.data.description}
  </p>
  <span class="text-sm font-medium text-[var(--color-primary-600)]">
    Learn more &rarr;
  </span>
</Card>
