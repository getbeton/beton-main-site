---
interface Props {
  href?: string;
  variant?: 'primary' | 'secondary' | 'ghost' | 'outline';
  size?: 'sm' | 'md' | 'lg';
  fullWidth?: boolean;
  class?: string;
  [key: string]: unknown;
}

const {
  href,
  variant = 'primary',
  size = 'md',
  fullWidth = false,
  class: className,
  ...rest
} = Astro.props;

const Tag = href ? 'a' : 'button';

const baseClasses =
  'inline-flex items-center justify-center font-bold border-2 uppercase tracking-wider transition-all duration-150 touch-action-manipulation focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-[var(--color-primary-500)] active:translate-x-0 active:translate-y-0 active:shadow-none';

const variantClasses = {
  primary: 'bg-[var(--color-primary-600)] text-white border-[var(--color-primary-600)] hover:bg-white hover:text-[var(--color-primary-600)] hover:shadow-[4px_4px_0_var(--color-primary-900)] hover:-translate-x-0.5 hover:-translate-y-0.5',
  secondary:
    'bg-[var(--color-neutral-100)] text-[var(--color-text)] border-[var(--color-text)] hover:bg-[var(--color-text)] hover:text-[var(--color-surface)] hover:shadow-[4px_4px_0_var(--color-text)] hover:-translate-x-0.5 hover:-translate-y-0.5',
  ghost: 'text-[var(--color-text-secondary)] border-transparent hover:text-[var(--color-text)] hover:border-[var(--color-text)] hover:shadow-[4px_4px_0_var(--color-text)] hover:-translate-x-0.5 hover:-translate-y-0.5',
  outline:
    'border-[var(--color-text)] text-[var(--color-text)] hover:bg-[var(--color-text)] hover:text-[var(--color-surface)] hover:shadow-[4px_4px_0_var(--color-text)] hover:-translate-x-0.5 hover:-translate-y-0.5',
};

const sizeClasses = {
  sm: 'px-3 py-1.5 text-sm gap-1.5',
  md: 'px-4 py-2 text-sm gap-2',
  lg: 'px-6 py-3 text-base gap-2',
};
---

<Tag
  href={href}
  class:list={[
    baseClasses,
    variantClasses[variant],
    sizeClasses[size],
    fullWidth && 'w-full',
    className,
  ]}
  {...rest}
>
  <slot />
</Tag>
